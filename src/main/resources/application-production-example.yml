spring:
    datasource:
        url:
        username:
        azure:
            passwordless-enabled: true
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
        hibernate:
            ddl-auto: create-drop